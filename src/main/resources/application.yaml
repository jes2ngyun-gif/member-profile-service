# 모든 환경에서 공통으로 사용하는 설정
spring:
  application:
    name: member-profile-service

  profiles:
    default: local       # ← 기본 스위치를 local에 둔다 (내 PC에선 H2)

management:
  endpoints:
    web:
      exposure:
        include: health   # ← /actuator/health 를 외부에 노출 (발제 요구사항)